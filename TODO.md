# Fix ImportError in models_db.py

1. [x] Define all SQLAlchemy models (User, EmailLoginCode, Conversation, Message) in app/models_db.py matching existing DB schema and code usage.
2. [x] Test app startup with docker compose up.
3. [ ] Verify /auth and /health endpoints.
4. [ ] Generate alembic migration if schema changes needed.
5. [ ] Mark complete.
